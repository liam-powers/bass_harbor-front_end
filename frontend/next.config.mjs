/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["www.talkbass.com"],
        formats: ["image/webp"],
    },
};

export default nextConfig;
